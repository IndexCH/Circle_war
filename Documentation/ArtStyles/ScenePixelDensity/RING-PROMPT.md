# 圆环细节密度修正

内置 image_gen，编辑原 `salt-crust-ring.png`；现有游戏截图仅作为配色与使用场景参考。
输出：`exec-0d4c350b-bb0d-418c-97ba-bf9f8bc59baf.png`。原图尺寸 1254 × 1254，生成尺寸 1254 × 1254。

Use case: precise-object-edit / pixel detail refinement. Image 1 is the EDIT TARGET, an existing complete circular salt-rock terrain rim sprite. Image 2 is CONTEXT ONLY for palette and game usage; do not include any of its objects, background, UI, player or text. The target ring's baked pixel clusters are much too coarse when displayed about 930 screen pixels in diameter. Repaint ONLY the surface detail and edge sampling at a much FINER logical pixel resolution. Design this complete ring on a 640 by 640 logical-pixel grid, with tiny 1 to 2 logical pixel cracks and small 2 to 5 logical pixel salt chips. NOT a 128 or 192 pixel ring enlarged. Keep the exact same circular silhouette, center, diameter relative to canvas, narrow band thickness and irregular rocky outline as image 1. Keep the upper pale gray-white salt lip and dark neutral charcoal/taupe inner side. Match the current cold-neutral gray-white and muted taupe palette exactly. No yellow, gold, orange, blue glow or metallic surface. Detail must be crisp and materially richer: numerous fine subtle mineral flecks, fine fissures, small flat-shaded rock facets; remove the current giant square checker-like blocks. About 20 to 26 logical pixels across the rock band, not big chunky boulders. Restrained game pixel art, no blurry painterly gradients, no grainy photo noise. Output only this entire continuous closed circular ring on a square canvas, near edge-to-edge like target with 1 percent margin, centered, no clipping. All background OUTSIDE the ring and INSIDE the big hole must be perfectly flat pure neon green #00FF00 for keying. Do not draw any green onto the stone. No shadows outside the ring, no checkerboard, no transparent checker pattern, no scenery, no floor inside the hole, no letters, symbols, characters or UI.
